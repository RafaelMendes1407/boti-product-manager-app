services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: product_manager
      POSTGRES_USER: product_manager
      POSTGRES_PASSWORD: localpassword
    ports:
      - "5432:5432"
